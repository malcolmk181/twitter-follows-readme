<div id="<%= dom_id tweet %>">
  <p>
    <strong>User:</strong>
    <%= link_to tweet.user.username, user_path(tweet.user) %>
  </p>

  <p>
    <strong>Content:</strong>
    <%= tweet.content %>
  </p>

</div>
